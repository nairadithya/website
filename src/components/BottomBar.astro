<div class="bottom-bar">
    <div class="quote">What do you despise? By this are you truly known.</div>
</div>

<style>
    .bottom-bar {
        grid-column: 1 / -1;
        border: 1px solid var(--accent);
        border-top: 0;
        min-height: 4em;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0.5em 1em;
    }

    .quote {
        font-style: italic;
        font-family: var(--font-serif);
        font-size: var(--size-base);
        text-align: center;
        text-wrap: balance;
    }

    @media (max-width: 880px) {
        .bottom-bar {
            min-height: 0;
            grid-column: auto;
            padding: 0.8rem 1rem;
        }

        .quote {
            font-size: var(--size-xs);
        }
    }
</style>
